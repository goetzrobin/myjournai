# session-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build session-server` to build the library.

## Running unit tests

Run `nx test session-server` to execute the unit tests via [Vitest](https://vitest.dev/).
